<!DOCTYPE html>
<html>
<html lang="en">

<head>
    <title>Party List</title>
    <meta charset="utif-8">
</head>

<body>
    <h1> List of Guests</h1>
    <% if (Object.keys(list).length == 0) { %>
    No people yet!
    <% } else { %>
    <ul>
        <% for (key in list) { %>
            <% let person = list[key]; %>
            <li><%= person.fname + " " + person.lname + ", " + person.food +", " + person.artime %></li>
        <% } // end for loop %>
    </ul>
    <% }  // end else %>
    <a href="/">Add people</a>
</body>

</html>
